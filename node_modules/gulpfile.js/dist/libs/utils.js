"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * List of supported modules.
 * @type {string[]}
 */
exports.modules = [
    "browserify",
    "browsersync",
    "clean",
    "favicon",
    "fonts",
    "images",
    "javascript",
    "pug",
    "sass",
    "sprites",
    "svgstore",
    "typescript",
    "webpack",
];
/**
 * Different steps in tasks (ordered).
 * @type {string[]}
 */
exports.steps = ["lint", "build", "watch"];
/**
 * Explode name separated by two points in 3 elements.
 *
 * @param {string} task
 * @return {TaskNameElements}
 */
function explodeTaskName(task) {
    let [type = "", name = "", step = ""] = task.split(":");
    if (exports.steps.indexOf(type) >= 0 || type === "start") {
        step = type;
        type = "";
    }
    else if (exports.steps.indexOf(name) >= 0 || name === "start") {
        step = name;
        name = "";
    }
    return {
        type,
        name,
        step,
    };
}
exports.explodeTaskName = explodeTaskName;
/**
 * Filter object with all elements that pass the test.
 *
 * @param {{}} obj
 * @param {(o: unknown, key: string) => boolean} predicate
 * @returns {{}}
 */
function filterObject(obj, predicate) {
    return Object.keys(obj)
        .filter(key => predicate(obj[key], key)) // eslint-disable-line @typescript-eslint/no-explicit-any
        .reduce((res, key) => Object.assign(res, { [key]: obj[key] }), {}); // eslint-disable-line @typescript-eslint/no-explicit-any
}
exports.filterObject = filterObject;
//# sourceMappingURL=utils.js.map